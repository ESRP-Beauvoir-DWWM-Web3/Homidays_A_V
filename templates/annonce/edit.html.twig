{% extends 'base.html.twig' %}


  {% if category is defined %}
        {% set title, name = 'annonce', 'annonce' %}

 {% endif %}

{% block title %}Mise à jour {{ title }}{% endblock %}

{% block body %}
    <h1> Mise à jour  {{ title }}</h1>

    {{ include('annonce/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_annonce_index') }}">Retour à la liste</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
