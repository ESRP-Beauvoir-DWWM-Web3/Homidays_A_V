{% extends 'base.html.twig' %}

  {% if destination is defined %}
        {% set title, name = 'destination', 'destination' %}


     {% endif %}
{% block title %}Mettre à destination{% endblock %}

{% block body %}
    
    {{ include('destination/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_destination_index') }}">Retour à la liste</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
